{
  "name": "bartleby",
  "private": true,
  "description": "Bartleby workspace root (tooling for the committed API contract).",
  "scripts": {
    "gen:contract": "openapi-typescript packages/contract/openapi.json -o packages/contract/types.ts"
  },
  "devDependencies": {
    "openapi-typescript": "^7.4.0",
    "typescript": "^5.6.0"
  }
}
